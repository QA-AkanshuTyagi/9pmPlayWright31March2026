// Reverse whole string from the sentence

let name = "I want to learn playwright" //op - thgirwyalp nrael ot tnaw I
let rev = " "

for(i = name.length-1; i>=0; i--)
{
    rev = rev + name[i]
}

console.log(rev)